<!DOCTYPE html>
<html lang="en">
    <%-include("./partials/head.ejs")%>

<body>
    <%-include("./partials/nav.ejs")%>
  
        
    <h4 class="id">
        <%=presi.id%>
    </h4>
    <h3 class="last">
        <%=presi.lastName%> <%=presi.firstName%>
    </h3>
    <h4 class="title">
        <%=presi.title%>
    </h4>
    <h4 class="title">
        <%=presi.content%>
    </h4>
    <a href="http://localhost:3000/president/create" class="edit" data ="<%=presi.id%>">update</a>
    <a href="http://localhost:3000/" class="delete" data ="<%=presi.id%>">delete</a>
    

    <%-include("./partials/footer.ejs")%>

    <!-- <script>
        
        const delet=document.querySelector('a.delete')
        console.log(delet);
        delet.addEventListener('click',(e)=>{
            event.preventDefault()
            const id = delet.getAttribute('data');
            console.log(`id is ${id}`);
            
            
        })
        
    </script> -->

    <%=presi.id%>
    <a href="http://localhost:3000/">yahoo</a>

</body>

</html>